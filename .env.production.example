# Deel D.4 — copy to .env.production and fill in. Never commit the real file.
# Build with: vite build --mode production

VITE_APP_ENV=production

VITE_SUPABASE_URL=https://<production-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<production-anon-key>

# Intentionally absent in production: mail goes to the real recipient
# (D.7). The mail guard throws if a whitelist is set here.
